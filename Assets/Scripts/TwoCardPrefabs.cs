using UnityEngine;

public class TwoCardPrefabs : MonoBehaviour
{
    public GameObject card1;
    public GameObject card2;

    // Start is called before the first frame update
    private void Start()
    {
        // card1 = GetComponent<GameObject>();
        //  card2 = GetComponent<GameObject>();
    }

    // Update is called once per frame
    private void Update()
    {
    }
}
