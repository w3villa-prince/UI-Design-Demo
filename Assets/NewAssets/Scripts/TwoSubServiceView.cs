using UnityEngine;

public class TwoSubServiceView : MonoBehaviour
{
    // Start is called before the first frame update

    public GameObject _subServicePrefab1;
    public GameObject _subSerVicePrefab2;

    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}
